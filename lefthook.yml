commit-msg:
  scripts:
    autofill-ticket:
      runner: bash
pre-commit:
  commands:
    standard:
      files: git diff --name-only master
      glob: "*.{rb}"
      run: bundle exec standardrb {files}
post-checkout:
  scripts:
    bundle-check-install:
      runner: bash
